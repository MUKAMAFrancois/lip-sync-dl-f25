# --- Core ML & Data ---
numpy==1.26.4          # Strictly pinned to prevent Numba/Coqui crashes
torch>=2.1.0
pandas
tqdm
scipy

# --- Audio & Video Processing ---
ffmpeg-python
opencv-python
pydub
librosa==0.10.1        # Pinned for stability
soundfile

# --- Pipeline Components (Inference) ---
openai-whisper
# CRITICAL FIX: Pin transformers to stop the download loop
transformers==4.42.4
sentencepiece
coqui-tts              # Now it will accept the transformers version above
demucs

# --- Wav2Lip Training & Evaluation ---
face-alignment
scikit-image
pytorch-fid
torchvision

# --- Utilities ---
yt_dlp
wget
xmltodict

# pip install coqui-tts demucs face-alignment scikit-image pytorch-fid torchvision yt_dlp wget xmltodict